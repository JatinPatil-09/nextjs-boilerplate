/**
 * Utils Barrel Export
 *
 * Export all utility functions from this file for clean imports.
 * Usage: import { formatDate, validateEmail, debounce } from "@/utils"
 *
 * Naming Convention:
 * - Utility files: kebab-case (e.g., date-utils.ts, string-utils.ts)
 * - Function names: camelCase (e.g., formatDate, parseUrl, validateEmail)
 */

// Date & Time Utilities
// export { formatDate, parseDate, getRelativeTime } from "./date-utils";

// String Utilities
// export { capitalize, truncate, slugify } from "./string-utils";

// Validation Utilities
// export { validateEmail, validatePhone, validateUrl } from "./validation-utils";

// Array Utilities
// export { groupBy, sortBy, unique, chunk } from "./array-utils";

// Object Utilities
// export { pick, omit, deepMerge, isEmpty } from "./object-utils";

// Number Utilities
// export { formatCurrency, formatNumber, randomBetween } from "./number-utils";

// Browser Utilities
// export { copyToClipboard, downloadFile, detectDevice } from "./browser-utils";

// Performance Utilities
// export { debounce, throttle, memoize } from "./performance-utils";

// Note: Uncomment and update exports as utility functions are added
export {};
